#include "GUI_#{Panel_Name}.h"

using namespace RoR;
using namespace GUI;

#{Panel_Name}::#{Panel_Name}(MyGUI::Widget* _parent)
{
	initialiseByAttributes(this, _parent);
}

#{Panel_Name}::~#{Panel_Name}()
{
}
